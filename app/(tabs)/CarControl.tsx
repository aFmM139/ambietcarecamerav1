import { View, Text } from "react-native";

export default function CarControl() {
  return (
    <View className="flex-1 items-center justify-center bg-black">
      <Text className="text-white">Control del Carro</Text>
    </View>
  );
}